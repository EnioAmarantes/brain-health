apiVersion: 1

providers:
  - name: BrainHealth Dashboards
    orgId: 1
    folder: BrainHealth
    type: file
    disableDeletion: false
    updateIntervalSeconds: 15
    options:
      path: /var/lib/grafana/dashboards
